
# Example backend environment variables for local development
# Copy this file to `.env` in the `backend/` folder and update values as needed.

# Postgres connection (use the `postgres` service when running with docker-compose)
# For safety, set `POSTGRES_PASSWORD` in a local `.env` or export it in your shell.

# Example strong password placeholder (replace before committing):
# POSTGRES_PASSWORD=ReplaceWithASecureRandomPassword123!
# Then set DATABASE_URL to use that password, for example:
# DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/mlm?schema=public

DATABASE_URL=postgresql://postgres:REPLACE_WITH_SECURE_PASSWORD@postgres:5432/mlm?schema=public

# Redis connection (use the `redis` service when running with docker-compose)
REDIS_URL=redis://redis:6379

# JWT secret - change to a secure random string for production
JWT_SECRET=please-change-this

# Backend server port
PORT=4000

# Business configuration
PAIR_UNIT_BV=7000
DAILY_PAIR_CAP=10

# Optional: Cloudinary URL or other external service URLs
CLOUDINARY_URL=

