<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Receipt - {{receiptNo}}</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', Arial, sans-serif;
      background: #f0f4f8;
      color: #1e293b;
    }

    .receipt-container {
      max-width: 720px;
      margin: 20px auto;
      background: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
      border: 1px solid #e2e8f0;
    }

    /* ── Header ── */
    .header {
      background: linear-gradient(135deg, #0f172a 0%, #1e3a5f 100%);
      padding: 28px 36px;
      display: flex;
      justify-content: space-between;l
      align-items: center;
    }

    .brand h1 {
      color: #ffffff;
      font-size: 26px;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    .brand .tagline {
      color: #94a3b8;
      font-size: 13px;
      font-weight: 400;
      margin-top: 2px;
    }

    .receipt-badge {
      background: rgba(255,255,255,0.12);
      border: 1px solid rgba(255,255,255,0.25);
      color: #ffffff;
      padding: 10px 24px;
      border-radius: 8px;
      font-size: 22px;
      font-weight: 800;
      letter-spacing: 2px;
    }

    /* ── Meta Row ── */
    .meta-row {
      display: flex;
      justify-content: space-between;
      padding: 18px 36px;
      background: #f8fafc;
      border-bottom: 1px solid #e2e8f0;
      font-size: 14px;
    }

    .meta-row .label {
      color: #64748b;
      font-weight: 500;
    }

    .meta-row .value {
      color: #0f172a;
      font-weight: 600;
    }

    /* ── Body ── */
    .body {
      padding: 28px 36px;
    }

    .customer-line {
      font-size: 15px;
      color: #475569;
      margin-bottom: 20px;
      line-height: 1.6;
    }

    .customer-line strong {
      color: #0f172a;
      font-weight: 600;
    }

    /* ── Description Box ── */
    .description-box {
      background: #f1f5f9;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 18px 22px;
      margin-bottom: 24px;
      font-size: 13.5px;
      line-height: 1.7;
      color: #334155;
    }

    .description-box p {
      margin-bottom: 8px;
    }

    .description-box p:last-child {
      margin-bottom: 0;
    }

    .description-box strong {
      color: #0f172a;
    }

    /* ── Items Table ── */
    .items-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 24px;
    }

    .items-table th {
      background: #0f172a;
      color: #ffffff;
      padding: 12px 16px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      text-align: left;
    }

    .items-table th:last-child,
    .items-table td:last-child {
      text-align: right;
    }

    .items-table td {
      padding: 12px 16px;
      font-size: 14px;
      border-bottom: 1px solid #f1f5f9;
      color: #334155;
    }

    .items-table tr:last-child td {
      border-bottom: none;
    }

    .items-table .total-row td {
      background: #f8fafc;
      font-weight: 700;
      color: #0f172a;
      font-size: 15px;
      border-top: 2px solid #e2e8f0;
    }

    /* ── Amount Section ── */
    .amount-section {
      display: flex;
      gap: 20px;
      margin-bottom: 24px;
    }

    .amount-card {
      flex: 1;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 16px 20px;
    }

    .amount-card .card-label {
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      color: #64748b;
      margin-bottom: 6px;
    }

    .amount-card .card-value {
      font-size: 20px;
      font-weight: 700;
      color: #0f172a;
    }

    .amount-card .card-subtext {
      font-size: 12px;
      color: #64748b;
      margin-top: 4px;
      font-style: italic;
    }

    /* ── Two Column Layout ── */
    .two-col {
      display: flex;
      gap: 20px;
      margin-bottom: 24px;
    }

    .col-left, .col-right {
      flex: 1;
    }

    /* ── GST Box ── */
    .gst-box {
      background: #fffbeb;
      border: 1px solid #fde68a;
      border-radius: 8px;
      padding: 16px 20px;
    }

    .gst-box .gst-number {
      font-size: 16px;
      font-weight: 700;
      color: #92400e;
      margin-bottom: 6px;
    }

    .gst-box .gst-note {
      font-size: 11.5px;
      color: #a16207;
      line-height: 1.5;
    }

    /* ── Terms Box ── */
    .terms-box {
      background: #f0f9ff;
      border: 1px solid #bae6fd;
      border-radius: 8px;
      padding: 16px 20px;
      font-size: 12px;
      line-height: 1.7;
      color: #0c4a6e;
    }

    .terms-box .terms-title {
      font-weight: 700;
      font-size: 12px;
      margin-bottom: 6px;
      color: #075985;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* ── Computer Generated Note ── */
    .note {
      text-align: left;
      font-size: 11px;
      color: #94a3b8;
      padding: 0 36px;
      padding-bottom: 12px;
      font-style: italic;
    }

    /* ── Footer ── */
    .footer {
      background: linear-gradient(135deg, #0f172a 0%, #1e3a5f 100%);
      padding: 18px 36px;
      text-align: center;
    }

    .footer .contact {
      font-size: 13px;
      color: #e2e8f0;
      margin-bottom: 8px;
    }

    .footer .contact a {
      color: #38bdf8;
      text-decoration: none;
      font-weight: 500;
    }

    .footer .address {
      font-size: 11px;
      color: #94a3b8;
      letter-spacing: 0.5px;
      text-transform: uppercase;
    }
  </style>
</head>
<body>
  <div class="receipt-container">
    <!-- Header -->
    <div class="header">
      <div class="brand">
        <h1>Mayo Cosmic Care</h1>
      </div>
      <div class="receipt-badge">RECEIPT</div>
    </div>

    <!-- Meta Row -->
    <div class="meta-row">
      <div>
        <span class="label">Date : </span>
        <span class="value">{{date}}</span>
      </div>
      <div>
        <span class="label">Receipt No : </span>
        <span class="value">{{receiptNo}}</span>
      </div>
    </div>

    <!-- Body -->
    <div class="body">
      <!-- Customer -->
      <div class="customer-line">
        Received With Thanks From : <strong>{{customerName}}</strong>
      </div>

      <!-- Description -->
      <div class="description-box">
        <p>
          The Amount Of <strong>₹{{totalAmount}} /-</strong> is received against Purchase Order for 
          Mayo Cosmic Care Mattress. The Purchase Order Amount is <strong>Non Transferable</strong> 
          and <strong>Non Refundable</strong> and will be adjusted against your Final Payment for the product.
        </p>
        {{#if paymentDate}}
        <p>
          <strong>Please Note :</strong> Your order will be processed and dispatched within 7-10 business days 
          from the payment date.
        </p>
        {{/if}}
      </div>

      <!-- Items Table -->
      <table class="items-table">
        <thead>
          <tr>
            <th>#</th>
            <th>Product</th>
            <th>Qty</th>
            <th>Price (₹)</th>
            <th>Total (₹)</th>
          </tr>
        </thead>
        <tbody>
          {{#each items}}
          <tr>
            <td>{{this.index}}</td>
            <td>{{this.name}}</td>
            <td>{{this.quantity}}</td>
            <td>{{this.price}}</td>
            <td>{{this.total}}</td>
          </tr>
          {{/each}}
          <tr class="total-row">
            <td colspan="4">Grand Total</td>
            <td>₹{{totalAmount}}/-</td>
          </tr>
        </tbody>
      </table>

      <!-- Amount Cards -->
      <div class="amount-section">
        <div class="amount-card">
          <div class="card-label">Amount in Rs.</div>
          <div class="card-value">₹{{totalAmount}}/-</div>
        </div>
        <div class="amount-card">
          <div class="card-label">Amount in Words</div>
          <div class="card-value" style="font-size: 14px;">{{amountInWords}}</div>
        </div>
      </div>

      <!-- Two Column: GST + Terms -->
      <div class="two-col">
        <div class="col-left">
          {{#if userId}}
          <div class="amount-card" style="margin-bottom: 12px;">
            <div class="card-label">User ID</div>
            <div class="card-value" style="font-size: 14px;">{{userId}}</div>
          </div>
          {{/if}}
          {{#if paymentDate}}
          <div class="amount-card">
            <div class="card-label">Payment Date</div>
            <div class="card-value" style="font-size: 14px;">{{paymentDate}}</div>
          </div>
          {{/if}}
        </div>
        <div class="col-right">
          <div class="terms-box">
            <div class="terms-title">Terms & Conditions</div>
            By proceeding with the payment, the customer agrees to these terms and conditions.
            The purchase amount is non-refundable and non-transferable. 
            Products will be delivered as per our standard delivery policy.
            For any queries, please contact our support team.
          </div>
        </div>
      </div>

      {{#if gstNo}}
      <!-- GST Info -->
      <div class="gst-box">
        <div class="gst-number">GST NO: {{gstNo}}</div>
        <div class="gst-note">Please Note GST Invoice Will be Generated for the P.O. Amount On Purchase of Product.</div>
      </div>
      {{/if}}
    </div>

    <!-- Computer Generated Note -->
    <div class="note">
      * This is a computer generated statement, hence signature is not required.
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="contact">
        <strong>Website :</strong> <a href="https://mayocosmiccare.com">www.mayocosmiccare.com</a>
        &nbsp;&nbsp;|&nbsp;&nbsp;
        <strong>Email :</strong> <a href="mailto:{{contactEmail}}">{{contactEmail}}</a>
      </div>
      {{#if companyAddress}}
      <div class="address">{{companyAddress}}</div>
      {{/if}}
    </div>
  </div>
</body>
</html>
